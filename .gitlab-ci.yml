image: dxjoke/docker-angular-ci-build
stages:
  - build
  - test
  - deploy
  
before_script:
  - npm install
  
app-build:
  stage: build
  artifacts:
    paths:
      - dist/
  script:
    - npm run build

unit-tests:
  stage: test
  script:
    - npm test